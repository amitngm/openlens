apiVersion: v1
kind: Service
metadata:
  name: qa-pr-dashboard-api
  namespace: qa-pr-dashboard
  labels:
    app: qa-pr-dashboard
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: qa-pr-dashboard
    component: api
