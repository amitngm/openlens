apiVersion: v1
kind: Service
metadata:
  name: qa-pr-dashboard-frontend
  namespace: qa-pr-dashboard
  labels:
    app: qa-pr-dashboard
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: qa-pr-dashboard
    component: frontend
