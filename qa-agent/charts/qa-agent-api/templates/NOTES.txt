🚀 QA Agent API has been deployed!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 Service Details:
   Name:      {{ include "qa-agent-api.fullname" . }}
   Namespace: {{ .Release.Namespace }}
   Type:      {{ .Values.service.type }}
   Port:      {{ .Values.service.port }}

📁 Data Storage:
{{- if .Values.persistence.enabled }}
   PVC:       {{ include "qa-agent-api.pvcName" . }}
   Size:      {{ .Values.persistence.size }}
   Mount:     /data
{{- else }}
   ⚠️  Using emptyDir (data will be lost on pod restart!)
{{- end }}

🔐 RBAC:
{{- if .Values.rbac.create }}
   ServiceAccount: {{ include "qa-agent-api.serviceAccountName" . }}
   Role:           {{ include "qa-agent-api.fullname" . }}
{{- else }}
   ⚠️  RBAC not created - using default ServiceAccount
{{- end }}

🔒 Network Policy:
{{- if .Values.networkPolicy.enabled }}
   ✅ Enabled - Only same-namespace pods can access this service
{{- else }}
   ⚠️  Disabled - Any pod can access this service
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🧪 Test the API:

   # Port forward (for local testing)
   kubectl port-forward svc/{{ include "qa-agent-api.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}

   # Health check
   curl http://localhost:8080/health

   # From another pod in same namespace
   curl http://{{ include "qa-agent-api.fullname" . }}:{{ .Values.service.port }}/health

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 Next Steps:

   1. Deploy the UI:
      helm install qa-agent-ui ./charts/qa-agent-ui -n {{ .Release.Namespace }} \
        --set qaAgentApi.serviceName={{ include "qa-agent-api.fullname" . }}

   2. View logs:
      kubectl logs -l app.kubernetes.io/name={{ include "qa-agent-api.name" . }} -n {{ .Release.Namespace }}

   3. Check PVC:
      kubectl get pvc -n {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
