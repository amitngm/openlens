apiVersion: v1
kind: Service
metadata:
  name: {{ include "qa-agent-ui.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qa-agent-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qa-agent-ui.selectorLabels" . | nindent 4 }}
